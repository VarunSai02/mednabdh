# Copy to .env and update for local development
DATABASE_URL=postgres://postgres:postgres@localhost:5432/nabdh
# If connecting to Azure Database for PostgreSQL, set the DATABASE_URL to the
# connection string provided by Azure (include user and password). Example:
# postgres://<dbuser>@<server>:<password>@<server>.postgres.database.azure.com:5432/nabdh?sslmode=require
# Or set `DB_SSL=true` to force SSL with default `rejectUnauthorized=true`.
DB_SSL=true
DB_SSL_REJECT=true
DB_POOL_MAX=20

PORT=4000
JWT_SECRET=please_change_this_to_a_secure_random_value

# Production notes:
# - Store the DATABASE_URL and JWT_SECRET in Azure Key Vault.
# - Grant your App Service or Container App a Managed Identity and allow it
#   to access Key Vault secrets. Use Key Vault to inject these values into
#   the App Service configuration at runtime.
